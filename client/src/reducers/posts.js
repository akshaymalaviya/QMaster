export default (state=[],action)=>{
    switch (action.type) {
        case 'FETCH_ALL':
            return state;
            break;
            case 'CREATE':
                return state;
                break;
        default:
            break;
    }
}